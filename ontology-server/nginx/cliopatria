server {
    listen %PUBLIC_PORT%;
    listen [::]:%PUBLIC_PORT%;

    server_name %PUBLIC_HOST%;

    location / {
      proxy_pass         http://cliopatria:3020;
      proxy_redirect     off;
      proxy_set_header   Host $host;
    }
}
